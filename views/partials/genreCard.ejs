<div class="quarter genres-card">
  <h4 class="browse-by-genre header-font">Browse by your favorite genre:</h4>

  <div class="genre-container">
    <% genres.forEach(genre => { %>
    <a href="/browse/<%= genre %> ">
      <button class="pills pill-btn content-font"><%= genre %></button>
    </a>
    <% }) %>
  </div>
</div>
